#include<stdio.h>
int main()
{
	int n;
	int i;
	printf("完数有：");
	for (n = 2;n < 1000;n++)
	{
		int s = 0;
		for (i = 1;i < n;i++)
		{
			if (n % i == 0) s = s + i;
		}
		if (s == n) printf("%d ", n);
	}
	return 0;
}
