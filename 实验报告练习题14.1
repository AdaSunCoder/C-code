#include<stdio.h>
int main()
{
	int a[6],i,n;
	for(i=0;i<6;i++)
	{
	   a[i]=2*i;
	   printf("%d ",a[i]);
	}
	printf("\n"); 
	printf("请输入一个数"); 
	scanf("%d",&n);
	
	for(i=5;i>=0;i--)
	{
	
		if(a[i]<n) 
		{
		  a[i+1]=n;
		  break;
	    }
		else 
		a[i+1]=a[i];
    }
    for(i=0;i<7;i++)
    printf("%d ",a[i]);
    return 0;
 } 
